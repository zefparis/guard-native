// PulseGuard business logic — to be ported from pulseguard-app/src/pulseguard/
// Files to port: constants.ts, behaviorSummary.ts, vocalRanChallenge.ts,
// deviceMotionState.ts, api.ts
//
// These will be adapted from Vite/web APIs to React Native equivalents
// (e.g. import.meta.env → expo-constants, fetch stays the same).
